# NIRIKSHAK AI — Data Directory

Data store directory for raw CSV datasets, parquet files, and SQLite database storage.

## Contents
- `raw/`: Raw eSAKSHI CSV export files.
- `processed/`: Standardised & entity-resolved datasets.
- `db/`: SQLite database storage (`nirikshak.db`).

